# My Local Pitch + BookingBug
To install required dependencies run:

npm install

bower install

To start the server and watch files for changes run:

gulp

The widget is then accessible at:

http://localhost:8000/new_booking.html
